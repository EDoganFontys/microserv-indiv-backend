spring:
  main:
    allow-bean-definition-overriding: true
  rabbitmq:
    host: localhost
    password: guest
    port: 15672
    username: guest
    exchange: user.exchange
    queue: user.queue
    routingkey: user.routingkey
server:
  port: 4500
  servlet:
    context-path: /api/v1
