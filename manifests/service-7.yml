apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes2-c780"
    labels:
        app: "emrekubernetes2-c780"
spec:
    type: ClusterIP
    ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes2-c780"