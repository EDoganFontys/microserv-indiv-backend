apiVersion : apps/v1
kind: Deployment
metadata:
  name: "emrekubernetes-bb45"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "emrekubernetes-bb45"
  template:
    metadata:
      labels:
        app: "emrekubernetes-bb45"
    spec:
      containers:
        - name: "emrekubernetes-bb45"
          image: "emreregistry.azurecr.io/emrekubernetes"
          ports:
          - containerPort: 8083