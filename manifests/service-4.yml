apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-bccd"
    labels:
        app: "emrekubernetes-bccd"
spec:
    type: ClusterIP
    ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-bccd"