apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-bb45"
    labels:
        app: "emrekubernetes-bb45"
spec:
    type: ClusterIP
    ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-bb45"