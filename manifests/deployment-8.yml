apiVersion : apps/v1
kind: Deployment
metadata:
  name: "emrekubernetes2-4361"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "emrekubernetes2-4361"
  template:
    metadata:
      labels:
        app: "emrekubernetes2-4361"
    spec:
      containers:
        - name: "emrekubernetes2-4361"
          image: "emreregistry.azurecr.io/emrekubernetes2"
          ports:
          - containerPort: 8080