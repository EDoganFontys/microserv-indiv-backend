apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "emrekubernetes-acbc"
    labels:
        app: "emrekubernetes-acbc"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: emre-kubernetes8d74-emrekubernetes-acbc.23e95785158f46e48b06.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "emrekubernetes-acbc"
                servicePort: 8081