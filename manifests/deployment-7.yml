apiVersion : apps/v1
kind: Deployment
metadata:
  name: "emrekubernetes2-c780"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "emrekubernetes2-c780"
  template:
    metadata:
      labels:
        app: "emrekubernetes2-c780"
    spec:
      containers:
        - name: "emrekubernetes2-c780"
          image: "emreregistry.azurecr.io/emrekubernetes2"
          ports:
          - containerPort: 8083