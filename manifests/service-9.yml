apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes2-d568"
    labels:
        app: "emrekubernetes2-d568"
spec:
    type: ClusterIP
    ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes2-d568"