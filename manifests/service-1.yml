apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-4c0b"
    labels:
        app: "emrekubernetes-4c0b"
spec:
    type: ClusterIP
    ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-4c0b"