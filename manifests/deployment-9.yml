apiVersion : apps/v1
kind: Deployment
metadata:
  name: "emrekubernetes2-d568"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "emrekubernetes2-d568"
  template:
    metadata:
      labels:
        app: "emrekubernetes2-d568"
    spec:
      containers:
        - name: "emrekubernetes2-d568"
          image: "emreregistry.azurecr.io/emrekubernetes2"
          ports:
          - containerPort: 8081