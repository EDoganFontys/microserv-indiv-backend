apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-de93"
    labels:
        app: "emrekubernetes-de93"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-de93"