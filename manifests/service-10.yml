apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-b825"
    labels:
        app: "emrekubernetes-b825"
spec:
    type: ClusterIP
    ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-b825"