apiVersion : apps/v1
kind: Deployment
metadata:
  name: "emrekubernetes-4c0b"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "emrekubernetes-4c0b"
  template:
    metadata:
      labels:
        app: "emrekubernetes-4c0b"
    spec:
      containers:
        - name: "emrekubernetes-4c0b"
          image: "emreregistry.azurecr.io/emrekubernetes"
          ports:
          - containerPort: 8082