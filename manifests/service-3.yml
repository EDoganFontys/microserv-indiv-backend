apiVersion: v1
kind: Service
metadata:
    name: "emrekubernetes-acbc"
    labels:
        app: "emrekubernetes-acbc"
spec:
    type: ClusterIP
    ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
    selector:
        app: "emrekubernetes-acbc"